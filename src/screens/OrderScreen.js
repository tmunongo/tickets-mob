import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Order = (props) => {
  return (
    <View>
      <Text>A page for an order</Text>
    </View>
  )
}

export default Order

const styles = StyleSheet.create({})
